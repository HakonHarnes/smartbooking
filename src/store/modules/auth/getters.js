export default {
    verificationToken(state) {
        return state.verificationToken;
    },
    accessToken(state) {
        return state.accessToken;
    },
    user(state) {
        return state.user;
    },
    qr(state) {
        return state.qr;
    }
};
